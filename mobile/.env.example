EXPO_PUBLIC_API_URL=http://localhost:8000/api
EXPO_PUBLIC_ENV=development

# Google Maps API key for the Android map (react-native-maps).
# Create one at https://console.cloud.google.com (enable "Maps SDK for Android").
# Without it the Android build shows a placeholder instead of crashing.
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=
