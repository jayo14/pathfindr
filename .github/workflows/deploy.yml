name: PathFindr Deploy

on:
  push:
    branches: [main]

jobs:
  deploy-backend:
    name: Deploy to Render
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: |
          curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_URL }}"
